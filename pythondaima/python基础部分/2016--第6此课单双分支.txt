# 分支结构之单分支：
t = int(input("请输入天气情况："))
#  1：出太阳   打球     其他：  不出太阳
# if 条件：    如果条件成立，则执行缩进后的语句（被if管着的）
#             如果条件不成立，则不执行if控制的语句；
''' 
if t==1:
    print("打球！")
    #print("这句话是测试功能！")
print("这句话是测试功能！")

# 双分支结构：  两个选择，两者必选其一，只能选其一
# if之后的条件成立，执行if控制的语句，否则执行else控制的语句：
if t==1:
    print("打球！")
else:
    print("上课！")
print("测试！")
'''
# 从键盘输入一个数，判断这个是数正还是负数；

x = eval(input("请输入一个数："))
if x>=0:
    print("正数！")
else:
    print("负数！")
